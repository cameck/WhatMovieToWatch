<%=
  react_component 'Results', { movies: @movie_results,
                               watchListItems: @watchlist_items,
                               user_id: current_user.id,
                               genre: @genre
                             }
%>
